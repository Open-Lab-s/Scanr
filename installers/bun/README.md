# Bun Install

Bun uses the same npm wrapper package as npm.

```bash
bun install -g @openlabs/scanr_cli
```

This package downloads the correct prebuilt Scanr binary for the current platform during install.
